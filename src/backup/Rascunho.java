/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package backup;

/**
 *
 * @author edson
 */
public class Rascunho {

    /*
      public static void realizarBackup(String host, String port, String database, String user, String password, String backupPath) {
        try {
            String comando = "pg_dump -h " + host + " -p " + port + " -U " + user + " -d " + database + " -F c -b -v -f " + backupPath;
            ProcessBuilder processBuilder = new ProcessBuilder("cmd.exe", "/c", comando);
            processBuilder.environment().put("PGPASSWORD", password);

            Process process = processBuilder.start();
            int exitCode = process.waitFor();

            if (exitCode == 0) {
                System.out.println("Backup realizado com sucesso.");
            } else {
                System.out.println("Erro ao realizar backup. Código de saída: " + exitCode);
            }
        } catch (IOException | InterruptedException e) {
            e.printStackTrace();
        }
    }

     */ 
    
     }

